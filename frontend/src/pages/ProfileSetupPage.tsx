import { PlaceholderPage } from '../components/PlaceholderPage'

export function ProfileSetupPage() {
  return (
    <PlaceholderPage
      name="Profile setup"
      path="/profile/setup"
      purpose="Complete profile details, one-time platform role selection, and registered skills."
    />
  )
}
