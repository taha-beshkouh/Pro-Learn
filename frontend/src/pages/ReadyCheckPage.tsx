import { PlaceholderPage } from '../components/PlaceholderPage'

export function ReadyCheckPage() {
  return (
    <PlaceholderPage
      name="Ready Check"
      path="/ready-check"
      purpose="Review and respond only to the authenticated member's current Ready Check invitation."
    />
  )
}
