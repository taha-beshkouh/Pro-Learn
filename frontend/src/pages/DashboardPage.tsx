import { PlaceholderPage } from '../components/PlaceholderPage'

export function DashboardPage() {
  return (
    <PlaceholderPage
      name="Dashboard"
      path="/dashboard"
      purpose="Summarize the authenticated member's active ProjectRun and backend-provided next action."
    />
  )
}
