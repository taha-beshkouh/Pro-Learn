import { PlaceholderPage } from '../components/PlaceholderPage'

export function ProjectDetailPage() {
  return (
    <PlaceholderPage
      name="Project detail"
      path="/projects/:projectVersionId"
      purpose="Review project information, role requirements, prerequisites, stacks, and Sprint outline."
    />
  )
}
