import { PlaceholderPage } from '../components/PlaceholderPage'

export function ProjectCatalogPage() {
  return (
    <PlaceholderPage
      name="Project catalog"
      path="/projects"
      purpose="Browse backend-published projects and filter them by supported level."
    />
  )
}
