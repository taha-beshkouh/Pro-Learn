import { PlaceholderPage } from '../components/PlaceholderPage'

export function WorkspacePage() {
  return (
    <PlaceholderPage
      name="Workspace"
      path="/workspace"
      purpose="Expose the active ProjectRun overview, team, Sprints, and static project resources."
    />
  )
}
